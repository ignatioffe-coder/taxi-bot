# 🚕 Taxi Forecast Bot

Telegram-бот для таксистов Москвы. Собирает данные о коэффициентах по районам и помогает понять, где и когда выгоднее работать.

## Возможности

- 📍 Приём коэффициентов от водителей по районам
- 📊 Рекомендации «куда лучше ехать сейчас»
- 📈 Личная статистика
- 🏆 Топ высоких коэффициентов
- 🗺️ Карта спроса
- 🌡️ Погода в Москве
- 🚗 Анализ пробок (по времени суток)

## Быстрый старт

1. Клонируй репозиторий:
   ```bash
   git clone https://github.com/ignatioffe-coder/taxi-bot.git
   cd taxi-bot
   ```

2. Создай виртуальное окружение и установи зависимости:
   ```bash
   python -m venv venv
   source venv/bin/activate   # Linux/macOS
   # или venv\Scripts\activate  # Windows
   pip install -r requirements.txt
   ```

3. Создай файл `.env`:
   ```env
   BOT_TOKEN=твой_токен_от_BotFather
   ```

4. Запусти бота:
   ```bash
   python bot.py
   ```

## Структура проекта

- `bot.py` — основной файл бота (хендлеры, клавиатуры)
- `config.py` — конфигурация
- `database.py` — работа с SQLite
- `analytics.py` — рекомендации и статистика
- `heatmap.py` — генерация карты спроса
- `traffic_analyzer.py` — анализ пробок
- `weather.py` — получение погоды (вспомогательный)

## Важно

- **Никогда** не коммить `.env` и токен бота.
- SQLite подходит для локальной разработки. На Railway/Heroku данные будут теряться при редеплое — лучше перейти на PostgreSQL.
- Админ-команда: `/admin` (только для ID из `ADMIN_IDS`).

## Лицензия

MIT
